from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A small ml project to detect whether a person is survived in titanic or what is the reason behind this',
    author='ravish',
    license='',
)
